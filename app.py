# *** Imports create_app from petfax folder
from petfax import create_app

# * Creates an app instance in the app.py file by invoking the function and saving it to a variable called app
app = create_app()